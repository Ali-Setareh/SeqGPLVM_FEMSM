\section{Sequential Gaussian processes latent variable model}
\label{sec:seqgplvm}
The idea behind the sequential Gaussian process latent variable model is to 
model the treatment dynamics using condition Markov model (CMM). latent variable
that captures the unmeasured confounding. 